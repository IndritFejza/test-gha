name: Test Output Overflow Layered Deep
on:
  workflow_dispatch:
jobs:
  big-output:
    uses: ./.github/workflows/test-overflow-inner.yaml  # <-- must be reusable workflow call
  downstream:
    needs: big-output
    runs-on: ubuntu-latest
    steps:
      - run: echo "I ran"
