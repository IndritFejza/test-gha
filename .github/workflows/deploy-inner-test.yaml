name: Test Inner String Input

on:
  workflow_call:
    inputs:
      input-string:
        type: string
        default: default-value
        required: true
        description: |
          input-string to test if-condition in step-level
jobs:
  test-input-handling:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      INPUT_STRING: ${{ inputs.input-string }} 
    steps:
      - name: Tag Testrunner Image
        if: env.INPUT_STRING != ''
        shell: bash
        run: |
          if [ -z "$INPUT_STRING" ]; then
            echo "input variable $INPUT_STRING is not empty"
          echo "Running test with input: ${{ inputs.input-string }}"
